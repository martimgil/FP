def primesUpTo(n):
    conj = set(range(2,n+1))
    to_remove=[]
    primes=set(range(2, n+1))
    for i in range(2, int(n**0.5)+1):
        if i in primes:
            multiples = set
    return conj

print(primesUpTo(20))